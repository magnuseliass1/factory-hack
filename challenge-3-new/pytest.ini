[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Async test settings
asyncio_default_fixture_loop_scope = function

# Output settings
addopts = 
    -v
    --tb=short
    --strict-markers
